public class ContactTriggerHandler {
    
    public static void afterInsert(Map<Id,Contact> newContacts) {
        
        //dml on account
        //dml on user
        //FutureMethods.createUsers(newContacts.keySet());
        //FutureMethods.updateUser();
    }
    
}